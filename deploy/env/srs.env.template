# ----------------------------
# SRS Service Configuration (Phase 3 — Spaced Repetition)
# ----------------------------
# Этот файл генерируется автоматически из .env
# НЕ редактируйте вручную!
# ----------------------------

# gRPC Server
GRPC_HOST=${SRS_GRPC_HOST}
GRPC_PORT=${SRS_GRPC_PORT}

# Logger
LOGGER_LEVEL=${SRS_LOGGER_LEVEL}
LOGGER_AS_JSON=${SRS_LOGGER_AS_JSON}

# PostgreSQL
POSTGRES_HOST=${SRS_POSTGRES_HOST}
POSTGRES_PORT=${SRS_POSTGRES_PORT}
POSTGRES_DB=${SRS_POSTGRES_DB}
POSTGRES_SCHEMA=${SRS_POSTGRES_SCHEMA}
POSTGRES_USER=${SRS_POSTGRES_USER}
POSTGRES_PASSWORD=${SRS_POSTGRES_PASSWORD}

# Cron — daily skill decay (Phase 3 full). UTC HH:MM. По умолчанию 01:00.
CRON_DAILY_AT=${SRS_CRON_DAILY_AT}
