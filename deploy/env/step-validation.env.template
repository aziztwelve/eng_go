# ----------------------------
# Step Validation Service Configuration (Phase 2)
# ----------------------------
# Этот файл генерируется автоматически из .env
# НЕ редактируйте вручную!
# ----------------------------

# gRPC Server
GRPC_HOST=${STEP_VALIDATION_GRPC_HOST}
GRPC_PORT=${STEP_VALIDATION_GRPC_PORT}

# Logger
LOGGER_LEVEL=${STEP_VALIDATION_LOGGER_LEVEL}
LOGGER_AS_JSON=${STEP_VALIDATION_LOGGER_AS_JSON}

# PostgreSQL
POSTGRES_HOST=${STEP_VALIDATION_POSTGRES_HOST}
POSTGRES_PORT=${STEP_VALIDATION_POSTGRES_PORT}
POSTGRES_DB=${STEP_VALIDATION_POSTGRES_DB}
POSTGRES_SCHEMA=${STEP_VALIDATION_POSTGRES_SCHEMA}
POSTGRES_USER=${STEP_VALIDATION_POSTGRES_USER}
POSTGRES_PASSWORD=${STEP_VALIDATION_POSTGRES_PASSWORD}

# Зависимости (gRPC)
GAMIFICATION_SERVICE_ADDR=${STEP_VALIDATION_GAMIFICATION_ADDR}
COURSE_SERVICE_ADDR=${STEP_VALIDATION_COURSE_ADDR}
SRS_SERVICE_ADDR=${STEP_VALIDATION_SRS_ADDR}
